{
    "name": "com.myorg.myPage",
    "scripts": {
        "build": "mbt build --mtar com.myorg.myPage.mtar",
        "build:workspaces": "npm run build --workspaces --if-present",
        "deploy": "cf deploy mta_archives/com.myorg.myPage.mtar --retries 0",
        "lint": "npm run lint --workspaces --if-present",
        "ui5lint": "npm run ui5lint --workspaces --if-present",
        "start:myPageApp": "npm start --workspace myPageApp"
    },
    "dependencies": {
        "mbt": "^1"
    },
    "workspaces": [
        "myPageApp"
    ]
}